#prime in a range
r=int(input("enter the range"))
for num in range(2,r+1):
    ip=True
    for i in range(2,num):
        if num%i==0:
            ip=False
            break
    if ip:
        print(num)
